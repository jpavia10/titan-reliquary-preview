# CHANGELOG — theme-refresh branch

**Branch:** `theme-refresh` (local only — never pushed; Joseph decides about merging)
**Base:** `main` @ `fa2b985` (Publish 2026-09-24 14:32 PT orphan snapshot, generated output)
**Scope:** PRESENTATION ONLY. No data semantics changed, no data files copied or
snapshotted into the branch, nothing deleted (additive only).

## How this branch relates to the real pipeline

The published site is **generated output** of Joseph's `publish_all.sh` (runs on his
own machine at `/home/box/collection/gui/publish_all.sh`, which I do not have).
The app repo is not editable source data — `data/index.json`, `data/detail/`,
`version.json`, and the photo shards change dynamically at publish time.

- I **read** `data/index.json` / `version.json` to build the UI against real fields
  (`value.estimated_total`, `counts.*`, `photos.*`, `albums_glance[]`, `vault.moments[]`),
  but **never committed or altered** them. They are fetched at runtime exactly as before.
- The `?v=` cache-bust stamps in `index.html` and `version.json`'s `content_hash`
  are normally regenerated by `publish_all.sh`. Because this branch edits
  `styles.css` / `app.js`, I set a **local stamp `?v=theme-refresh-2`** on the
  `<link>`, `<script>` tags and `window.TITAN_BUILD`, and the same string as the
  service worker `BUILD`. **On merge, `publish_all.sh` must regenerate its own
  stamps as usual — do not keep `theme-refresh-2` in production.**
- New static assets (fonts, `js/playlist.js`, `audio.js`, `ambient.js`,
  `favicon.svg`) live **outside** `data/` so the publish pipeline cannot clobber them.
- `DRIVE_ANALYSIS.md` at the repo root is untracked review context from this
  morning's Drive analysis — not part of the theme work. Delete or keep at Joseph's call.

## What changed (file by file)

### `index.html`
- Added an inline `<script>` in `<head>` (before `styles.css` loads) that reads
  `localStorage.tr_theme_v1` and sets `data-theme` on `<html>` — no flash of the
  wrong theme on load/reload.
- Added a 3-way segmented theme toggle (Vault / Ledger / Noir) and a Rain button
  (inline SVG cloud icon) in the header actions, next to the Auto toggle.
- Added `favicon.svg` link (new file, gold-coin mark on dark).
- Replaced the board pane's empty shell and the "Loading vault…" header text with
  skeleton shimmer blocks (rendered until `app.js` replaces them with real data).
- Added `<script>` tags for `js/playlist.js`, `audio.js`, `ambient.js`.
- `?v=` stamps: `3f0bd0c319f0ef31` → `theme-refresh-2` (see stamp note above).

### `styles.css`
- New CSS variables for previously hardcoded surfaces (`--th-bg`, `--tabgrad1/2`,
  `--foot-bg`, `--ph-bg`, `--sel-bg`, `--hold-bg`, `--missing`, `--drawerbar-bg`,
  `--cardmeta`, `--skeleton1/2`, `--toastgrad1/2`) so themes recolor everything.
  The **vault (default) theme keeps the old look** — `data-theme` unset
  or `"vault"` preserves every original variable value (the CSS file itself
  changed, but the rendered vault theme is visually identical to before).
- `html[data-theme="ledger"]`: warm archival paper (`#f4efe4` bg, `#1a1611` ink,
  oxblood `#7a2e2e` accents, brass `#8a5a1e`). Paper-toned gradients, softened shadow.
- `html[data-theme="noir"]`: true black `#050505` + champagne `#e8d9a8`.
- `@font-face` for self-hosted **Fraunces** (weights 500/600/700, `fonts/`) — serif
  now on: brand h1, card section headings, board stat numbers (`.card .val`,
  `.stat .v`), dossier titles (`.ds-head h2`), album family cards, moment text.
  Body text and tables keep the system stack (readability for Dad's viewing).
- Card hover: `translateY(-2px)` + gold border glow (latest cards + album cards;
  plain cards get the border highlight on hover-capable devices).
- New component styles: theme toggle, rain button, skeleton shimmer, stat strip,
  moment card, album cards, dossier placard, lofi pill/player bar, rain canvas,
  ambient mixer panel.
- `prefers-reduced-motion`: all animations/transitions effectively disabled;
  moment rotation and rain canvas are skipped (sound still works).

### `app.js` (presentation only, no data logic touched)
- Theme system: `setTheme()` reads/writes `localStorage.tr_theme_v1`, updates
  `aria-pressed` on the toggle, and recolors the `theme-color` meta per theme.
- Header: the "grand" chip figure now **counts up** from 0 on load (1.1s ease-out;
  skipped under `prefers-reduced-motion`).
- Board: new **"Vault at a glance" stat strip** (existing fields only:
  `value.estimated_total`, `counts.vault`, `counts.countries`, `counts.flips`,
  `photos.coins_with_photos` as % of flips, newest flip via existing `latestFlips(1)`).
- Board: new **"From the vault"** card cycling `vault.moments[]` every 12s with a
  CSS fade (timer cleared on re-render; skipped under reduced motion).
- Board: new **"Top album families"** section — top 4 of `albums_glance[]` by coin
  count as compact cards (family, coins, total, pulse). Rows whose `family` has no
  letters are skipped at render time (one malformed row exists in the current
  snapshot — family `"$1247.71"` — this is a data-domain issue for Grok; the UI
  just refuses to render it as a family card).
- Dossier: header restyled as a **museum placard** (inner hairline frame, double
  gold rule under the header) with a new spec grid: SER · Year · Denom · Mint ·
  Est · Confidence. All existing sections/fields below are untouched.
- Skeleton shimmer in `index.html` is replaced by real content on `renderAll()`
  exactly as before; the "Awaiting photo" empty states are untouched first-class
  design elements.

### `fonts/` (new)
- `Fraunces-500.woff2`, `Fraunces-600.woff2`, `Fraunces-700.woff2` — latin subset,
  ~35 KB each. Licensed under the SIL Open Font License (Google Fonts). No CDN —
  the PWA works offline.

### `favicon.svg` (new)
- Gold-coin mark (ring + dashed inner ring + gold disc) on the vault-dark rounded
  square. Per the standing rule for Pages sites (title + icons + manifest).

### `js/playlist.js` (new)
- Static JS module setting `window.TITAN_PLAYLIST` — a plain array of
  `{title, artist, url, license, credit?}`. Kept outside `data/` so the publish
  pipeline cannot clobber it. Edit by hand to swap tracks.

### `audio.js` (new) — integrated lofi player
- Autoplay attempt on boot (autoplay preference persisted in
  `localStorage.tr_lofi_autoplay_v1`, default on). Browsers block unmuted
  autoplay — the rejection is caught and a **pulsing "Tap for lofi" pill** appears.
- AudioContext resumed on first touch/pointerdown (mobile Safari).
- Play/pause, next/prev, volume slider, now-playing title/artist, track counter,
  collapsible floating bar (bottom sheet on mobile, bottom-right corner on desktop,
  all touch targets ≥44px).
- Volume (`tr_lofi_vol_v1`) and last track (`tr_lofi_track_v1`) persist.
- Dead tracks are skipped gracefully with a toast; if every track fails it stops
  and says so instead of spinning.
- CC-BY tracks (if any) show their credit line in the player UI.

### `ambient.js` (new) — rainstorm + ambient mixer
- Header **Rain** button toggles (a) a looping rain sound and (b) a full-viewport
  canvas rain-streak overlay (`pointer-events:none`, z-index below UI). Toggle off
  stops both cleanly (cancels rAF, removes resize listener, pauses audio — no leaks).
- Under `prefers-reduced-motion` the canvas animation is skipped; the sound still works.
- Enabling rain reveals the **ambient mixer panel**: Rain / Distant thunder /
  Fireplace / Night wind, each with its own toggle + volume slider.
  Shift-click the Rain button opens the mixer without toggling rain.
- Ambience state persists in `localStorage.tr_ambient_v1`; previously-on sounds
  resume on next visit (autoplay-safe: a blocked resume just stays off).
- Rain and lofi use separate `<audio>` elements → they play simultaneously.

### `sw.js`
- `BUILD` updated to `theme-refresh-2`; precache list extended with `favicon.svg`,
  the three Fraunces woff2 files, `js/playlist.js`, `audio.js`, `ambient.js`.
  (Publish regenerates the stamp — see note above.)

## Audio track list (hotlinked — see offline note)

All 12 tracks: **Kevin MacLeod, incompetech.com, CC-BY 4.0** (credit line shown in
the player UI for each track). URLs are the exact MP3 paths read verbatim from
each track page's Download button `data-url` on 2026-09-25 and verified to serve
audio (full MP3 downloads succeeded during verification).

| # | Title | Artist | URL |
|---|-------|--------|-----|
| 1 | Chill Wave | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Chill%20Wave.mp3 |
| 2 | Chillin Hard | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Chillin%20Hard.mp3 |
| 3 | Suave Standpipe | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Suave%20Standpipe.mp3 |
| 4 | Ice Flow | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Ice%20Flow.mp3 |
| 5 | Soporific | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Soporific.mp3 |
| 6 | Kool Kats | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Kool%20Kats.mp3 |
| 7 | Ultralounge | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Ultralounge.mp3 |
| 8 | Mirage | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Mirage.mp3 |
| 9 | Local Forecast - Slower | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Local%20Forecast%20-%20Slower.mp3 |
| 10 | Disco Lounge | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Disco%20Lounge.mp3 |
| 11 | Easy Jam | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Easy%20Jam.mp3 |
| 12 | Airship Serenity | Kevin MacLeod | https://incompetech.com/music/royalty-free/mp3-royaltyfree/Airship%20Serenity.mp3 |

Credit format per incompetech (shown in player): `"[Track Title]" Kevin MacLeod
(incompetech.com) — Licensed under Creative Commons: By Attribution 4.0 License
http://creativecommons.org/licenses/by/4.0/`

Note: the plan's first-choice source was Pixabay Music, but Pixabay's direct
`cdn.pixabay.com/audio/...` URLs are embedded only in JavaScript-rendered page
data and could not be extracted with the available tooling (no URL was
fabricated). The task's documented fallback (incompetech) was used instead.

### Ambient sounds (hotlinked — see offline note)

Mixkit Sound Effects Free License (free for commercial and non-commercial use,
no attribution required — confirmed on mixkit.co 2026-09-25). URLs are the
player-preview MP3s Mixkit's own site streams (`assets.mixkit.co/active_storage/
sfx/{id}/{id}-preview.mp3`), copied verbatim and verified playable in Chromium
on 2026-09-25 (native audio player rendered with the listed durations).

| Sound | Mixkit title | Duration | URL |
|-------|--------------|----------|-----|
| Rain | Rain long loop | 0:57 | https://assets.mixkit.co/active_storage/sfx/2394/2394-preview.mp3 |
| Distant thunder | Thunder rumble during a storm | 0:51 | https://assets.mixkit.co/active_storage/sfx/2395/2395-preview.mp3 |
| Fireplace | Campfire crackles | 0:24 | https://assets.mixkit.co/active_storage/sfx/1330/1330-preview.mp3 |
| Night wind | Scary woods at night (tagged Night/Wind) | 0:50 | https://assets.mixkit.co/active_storage/sfx/2483/2483-preview.mp3 |

Note: Pixabay was the plan's first-choice source and four Pixabay SFX tracks
were verified by full download — but Pixabay's direct `cdn.pixabay.com/audio/...`
URL strings are embedded only in JavaScript-rendered page data and could not be
extracted with the available tooling (no URL was fabricated), and
`browser.open` cannot fetch pixabay.com (blocked by policy). Mixkit was used
instead; the Pixabay MP3s remain in the browser task's download folder if a
future pass wants to self-host them (see limitation #1).

## Known limitations / things Joseph & Grok must know

1. **Audio is hotlinked, not self-hosted.** MP3s stream from Pixabay's CDN
   (`cdn.pixabay.com`). They will NOT work offline and could break if Pixabay
   changes URLs. Nothing was committed to the repo (no binary audio) per the
   plan. If Joseph wants offline audio, the fix is to download the MP3s into a
   `media/` folder in the repo and point the playlist/ambient configs at them —
   say the word and I'll do it (adds ~50–80 MB to the repo; Pages can serve it).
2. **`?v=theme-refresh-2` stamps must be regenerated by `publish_all.sh` on merge.**
   Do not ship the literal string to production.
3. **The malformed `albums_glance` row** (family `"$1247.71"`, ids `"$75.00"`,
   coins 2072) is hidden from the new album-family cards at render time. It still
   exists in `data/index.json` — Grok's domain to fix in the pipeline.
4. **Live-update behavior is untouched** (30s poll / 10s drip / reload-on-stamp-change
   / toast) and works identically in all three themes.
5. Autoplay: first visit will almost always show the "Tap for lofi" pill (browser
   policy) — one tap starts it, and the preference persists afterward.
6. `DRIVE_ANALYSIS.md` (untracked, repo root) is review context from this morning,
   not part of this change.

## Verification
- `node --check` clean on `app.js`, `audio.js`, `ambient.js`.
- Served locally via `python3 -m http.server`; all new assets return 200
  (styles, app, audio, ambient, favicon, fonts). `js/playlist.js` pending track URLs.
- <!-- VERIFICATION: browser smoke test results go here -->

## tr4 — lofi resume fix + Stormroom ambient mixer (2026-09-25)

**audio.js — resume tracks the song, not the slot.** The player persisted only the
numeric track index (`tr_lofi_track_v1`), so after the Ultralounge reorder a phone
with a saved index resumed the wrong slot on reload. The key is now
`tr_lofi_track_v2` storing `index::title`; on boot, if the saved title no longer
sits in the saved slot the player resumes that song wherever it moved, else falls
back to track 1. A stale v1 key is migrated once (forced to track 1) and retired.
`node --check` clean; resume logic unit-tested in isolation (stale v1 → Ultralounge,
fresh → Ultralounge, valid v2 → same song).

**ambient.js — "Stormroom" mixer v2.** Web Audio graph (per-layer GainNode + master
gain) with ~1.4s fade in/out instead of hard cuts; Storm Intensity slider couples
rain loudness to the canvas rain (drop count/speed/opacity, drizzle→downpour) plus
slow wind gusts; one-tap presets (Storm / Fireside / Night watch / Off); master
volume; long-press the rain button to open the mixer (mobile-friendly; shift-click
kept on desktop); v1 localStorage state migrated to `tr_ambient_v2`; SVG layer
icons; bottom-sheet panel on mobile.

**Stamps:** all `?v=` → `tr4`, `TITAN_BUILD = "tr4"`, SW `BUILD = "tr4"` (the SW
precaches `audio.js?v=<BUILD>` cache-first, so the build bump is what evicts the
stale player from phones — this was the second half of the "wrong first song" bug).

## tr5 — QA defect fixes (2026-09-25)

Desktop visual QA (1920×1080, all three themes) found 11 defects; fixed the 9 in
presentation scope:
1. Lofi "Tap for lofi" pill / mini-player now tuck behind the dossier drawer and
   the ⌘K palette (new `titan:overlay` window event; audio.js hides and restores
   exactly what was showing). They no longer cover dossier notes or table headers.
2. Hero FLIPS tile subtext shortened ("2×2 holders · C### keys") — no orphan wrap.
3. Mini-player: counter `white-space: nowrap`, credit flexes with ellipsis, desktop
   bar widened 340→384px.
4. Age/year displays rounded to 1 decimal (was 6-decimal false precision).
5. Watchlist `splitFlags` no longer splits on `·` inside parentheses (the broken
   "A020 Sacagawea (investigate / 3 years locked)" row). Remaining near-duplicate
   rows are data-side (Grok's `vault.flags`) — flagged, not touched.
6. Typographic nits: "Albums · Age" tab spacing; non-collapsing footer separator.
7. Live-status "built X · checked Ns ago" wrapped in `.nowrap` — no orphan fragment.
8. (Not a defect — QA ran against tr3; the Stormroom preset/intensity panel it
   missed is the tr4 panel. Re-verified in the next pass.)
9. `.table-wrap` gets `margin-bottom: 4.5rem` so the fixed footer never covers the
   last rows; thin gold scrollbars on table-wrap, latest-rail, country-strip.

## tr6 — Complete-product framework expansion (2026-09-25)

Joseph's direction: tr3–tr5 were a stepping-stone; take the redesign all the way
home before he commits. Themes were "basic / AI slop" — they are now complete
atmospheres, and the framework anticipates Phase 2 (photo scan) instead of just
repainting. Presentation-only: `data/`, `version.json`, photo shards untouched.

**WS1 — Atmosphere system (retires the palette-toggle concept).** Vault/Ledger/Noir
replaced by two authored atmospheres: **After Hours** (true-black gallery, champagne
spotlighting, heavier grain — the dark concepts merged into one signature) and
**Conservator** (archival paper, oxblood stamps, ruled ledger lines — the Ledger idea
recast as a brighter, high-readability archival presentation). Noir retired as a
near-duplicate, not merely deleted. One Atmosphere button + picker sheet with full
descriptions; "Set the scene" pairs After Hours→Storm+lofi, Conservator→Fireside+lofi.
`tr_atmo_v1` storage; legacy `tr_theme_v1` migrated (ledger→conservator,
vault/noir→afterhours, tolerant of JSON-quoted values); first-visit light
`prefers-color-scheme` hints Conservator, saved choice always wins.
`audio.js` exposes `window.TitanLofi`; `ambient.js` exposes `window.TitanAmbient`.

**WS2 — Phase-2-ready dossier.** Completeness meter reframed: "Record X/Y ·
awaiting Phase 2: …" instead of "missing"; photo segment counts obverse/reverse
(0/2 with "obverse + reverse await Phase 2"). New **Phase 2 capture** section gives
designed homes to grade, provenance, source, weight, obverse/reverse photos with
intentional "Awaiting Phase 2" empty states — no values invented. Photo instructions
and file targets unchanged.

**WS3 — Collection intelligence (board).** "Collection intelligence · The vault,
thinking": top-10 value concentration (currently 33%), decade/age distribution
bars, top-country spread by count and value, silver count, and clickable "Hidden
gems" (est ≥ $5, confidence not high — currently 2) opening dossiers. Computed only
from real `vault.flips` fields; graceful fallbacks when fields are sparse.

**WS4 — Photo/QC command center (board).** Honest Phase-2 progress (currently
"0 of 273 flips photographed · 0%"), awaiting flips grouped by country, top-6
country sessions with count/represented value/silver, "Start session" sets Flips
filters (Phase 2 + country). Empty-start messaging; completion celebration is a
data-dependent follow-up once Phase 2 rows exist.

**WS5 — Bulletproofing.** Honest offline banner ("Offline — viewing the last
snapshot…"). Overlay manager: Escape closes the topmost layer; focus trapped in
sheets/palette/mixer/drawer and restored on close (also fixed a real bug: Escape
left focus stranded in the hidden palette input, which silently disabled `?` and
`1–6`). Keyboard: `?` shortcuts sheet, `1–6` tab jumps (roving tabindex,
aria-selected), tab-strip arrows, Ctrl/⌘K retained. Dossier print button; flips
inventory print button; print stylesheet renders black-on-white dossier/inventory.

**WS6 — Motion polish.** Shared `--ease-out` token; reveal/pane transitions moved
to it; bars transition in; reduced-motion block suppresses all new animation.

**Verification (2026-09-25, jsdom harness vs real `data/index.json`):** 37/37
checks pass, zero uncaught exceptions — hero `$5,584.11`, board renders, insights
math, sessions filter correctly (Mexico session → "60 / 273 · shooting list"),
dossier Phase-2 section, palette search (8 hits for "mexico"), atmosphere
switch + persistence + both "set the scene" pairings, shortcuts/Escape stack, tab
keys, legacy theme migration (ledger→conservator, vault/noir→afterhours),
offline/online banner. `node --check` clean on all five JS files; CSS braces
balanced; `BUILD = "tr5"` stamp untouched (no `?v=` bump — deployment handles it).

**Known limitations / needs a real browser:** jsdom has no layout engine, so
390×844 / 1440×900 overflow and true visual QA (grain, spotlighting, ruled paper
in both atmospheres) still need eyes. Lightbox with real photos can't be exercised
until Phase 2 shards exist. Service worker precache list verified by inspection
only (no new shell files were added, so the tr5 list still covers it).

## tr6 patch — pill-first lofi (2026-09-25, Joseph's call)

The lofi mini-player module no longer appears on page load. Only the "Tap for lofi"
pill shows at boot; the full player bar appears after the user taps the pill (or
starts audio via an atmosphere's "Set the scene"). Boot no longer attempts
autoplay. (audio.js only; smoke harness re-run: all pass.)

## tr8 — self-updating service worker (2026-09-25)

Joseph's phone was stuck showing the tr5 build (old theme toggle, bar+pill overlap)
because a tab left open never re-checked for a new service worker. The app now:
checks for a new SW every time the tab becomes visible, and reloads once (with a
toast) when a new SW takes control — so future deploys propagate on their own
instead of stranding users on stale UI. First install can't reload-loop (guarded
by prior-controller check). (app.js only.)

## tr9 — Full museum rebuild (2026-09-25)

Joseph rated the tr3–tr8 reskin 5/10: "I thought you were doing a full retheme
and revamp of the product?" This build stops treating Titan as a dashboard and
rebuilds it as a **reliquary / museum experience**:

- **Five wings, bottom navigation:** Grand Hall, Gallery, Vault, Curator's
  Study, Conservation Lab. Old tabs (Board/Flips/Bullion/World/Ops/Age) are
  gone; saved state and `#hash` deep links migrate via a legacy map.
- **Grand Hall:** cinematic "Now exhibiting" rotation (top-5 valued flips,
  9s crossfade, tap for dossier), wing entrance cards, an unmissable
  Conservation Lab banner (0 of 273 · 0%), Curator's notes, rotating "From
  the vault" editorial, watchlist.
- **Gallery:** flips as exhibit cards (photo or "Awaiting photo" monogram
  frame) — no more spreadsheet-first table. Filters/sort/search/country
  strip kept; tap a piece for its placard dossier; printable.
- **Vault:** bullion/sets/housing/stamps + silver ledger, unchanged data.
- **Curator's Study:** collection intelligence, precious-metal ledger, World
  country table, Age bands, albums glance, bucket breakdown.
- **Conservation Lab:** Phase-2 photo QC command center — shooting sessions
  (start → Gallery pre-filtered), Requests from Titan, Next IDs, Next SER.
- **Atmospheres as experiences:** After Hours is slow/cinematic/generous;
  Conservator is dense/archival/fast. Wing transitions differ per atmosphere.
- Keyboard 1–5, roving tabindex, focus trap/restore, Esc stack, print,
  offline banner, reduced-motion, service-worker self-update all preserved.
- Data model untouched: `data/index.json` etc. are still read-only
  generated inputs. All values computed from real fields; nothing invented.
